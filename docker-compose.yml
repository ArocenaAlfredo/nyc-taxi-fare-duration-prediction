services:
  api:
    build: .
    container_name: nyc-taxi-api
    ports:
      - "8000:8000"
    volumes:
      - ./models:/app/models
    command: uvicorn src.api.main:app --host 0.0.0.0 --port 8000